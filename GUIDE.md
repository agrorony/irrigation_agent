# מדריך עבודה עם הפרויקט – Irrigation Agent

## סקירה כללית
פרויקט זה מיישם סביבת למידה מחזקת (Reinforcement Learning) לתזמון השקיה חכם באמצעות API בסגנון Gymnasium.  
המטרה היא לאמן סוכן (agent) שמחליט *מתי* ו-*בכמה* להשקות על בסיס תנאים קלימטיים יומיים ומצב הקרקע.

הפרויקט בנוי בגישה **Notebook-centric**:
- סביבת ההשקיה מוגדרת ב־Notebook
- אלגוריתם הלמידה ממומש כמודול Python
- ניסויים, אימון וניתוח מתבצעים דרך Notebook ייעודי

---

## מבנה הקבצים

### 1. **irrigation_env.ipynb** 📓
**תפקיד**: הגדרת סביבת ההשקיה (Environment) ופיתוחה.

**מה יש בו**:
- מחלקה `IrrigationEnv` המממשת API בסגנון Gymnasium
- **State Space**:
  - `soil_moisture` ∈ [0,1]
  - `crop_stage` ∈ {0,1,2}
  - `rain` (מנורמל)
  - `et0` (מנורמל)
- **Action Space**:
  - 0 – ללא השקיה
  - 1 – השקיה קלה (5 מ״מ)
  - 2 – השקיה כבדה (15 מ״מ)

**לוגיקה מרכזית**:
- משוואת איזון מים:  
  `SM(t+1) = SM(t) + Irrigation + Rain − ET_crop`
- מקדם גידול (Kc) לפי שלב: `[0.5, 1.15, 0.7]`
- פונקציית תגמול עם קנסות על יובש, הצפה ועלות השקיה

---

### 2. **irr_Qtable.py** 🧮
**תפקיד**: מימוש אלגוריתם Q-Learning טבלאי.

**עיקרי המימוש**:
- דיסקרטיזציה:  
  `12 (soil) × 3 (crop) × 2 (ET₀) × 2 (rain) = 144 states`
- Q-learning סטנדרטי עם ε-greedy
- המשכיות בין אימונים מתבצעת בזיכרון (Notebook)

---

### 3. **experiments.ipynb** 🔬
**תפקיד**: אימון, ניסויים וניתוח של הסוכן.

**מבנה המחברת**:
1. Setup – טעינת קוד ויצירת סביבה
2. Training – אימון Q-table
3. Inspection – ניתוח מדיניות
4. Continued Training – המשך אימון מאותה טבלה

---

## זרימת עבודה מומלצת
1. הבנת הסביבה ב־`irrigation_env.ipynb`
2. אימון וניתוח ב־`experiments.ipynb`
3. התאמות בדיסקרטיזציה או פרמטרים ב־`irr_Qtable.py`

---

## טיפים
- להתחיל עם מעט אפיזודות (100–300)
- לבדוק התפלגות פעולות, לא רק reward
- לא לשנות state space בלי לאפס Q-table

---

## סיכום
- הסביבה – `irrigation_env.ipynb`
- האלגוריתם – `irr_Qtable.py`
- הניסויים – `experiments.ipynb`

הפרויקט מיועד ללמידה, ניסוי ודיון – לא לשימוש יצרני.
